

type UserPosition {
    "${product.id}:${user.id}"
    id: ID!

    user: User!
    chain: Chain!
    product: Product!

    shareQuantity: Float!
    valueUSD: Float!

    createdAtTimestamp: DateTime!
    pnl: Float!
}