
########################################################
# Price
########################################################

extend type Erc20Token {
  price: FloatDataPoint!
  lastPrice: Float!
}

extend type NativeToken {
  price: Float!
  priceChange24h: Float!
}

extend type ClassicVault {
  price: BigDecimal!
  priceChange24h: Float!
}

extend type ClmVault {
  price: BigDecimal!
  priceChange24h: Float!
}

extend type RewardPool {
  price: BigDecimal!
  priceChange24h: Float!
}

extend type LiquidStakingVault {
  price: BigDecimal!
  priceChange24h: Float!
}

extend type GovernanceVault {
  price: BigDecimal!
  priceChange24h: Float!
}

